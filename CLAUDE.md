# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Overview

**YS_smartthings** is a Samsung SmartThings integration project. This repository is newly initialized and has no source code yet. Update this file as the project structure is established.

## Getting Started

This repository is currently empty. As the project is built out, document the following here:

- Build commands (e.g., `npm install`, `npm run build`)
- Lint commands (e.g., `npm run lint`)
- Test commands (e.g., `npm test`, and how to run a single test)
- How to run the project locally

## Architecture

Document the high-level architecture here as it develops:

- What this integration does (devices/capabilities it exposes to SmartThings)
- How the code is organized (e.g., device handlers, automations, schemas)
- Key data flows between SmartThings and any external services

## Conventions

Document project-specific conventions here as they are established (e.g., naming patterns, file organization, SmartThings API version in use).
